# Runtime simulation outputs
PITOT3_Outputs/

# Legacy results folder (replaced by capital-R Results/, which is
# self-ignored via Results/.gitignore).  Kept here as a safety net so
# the dead path can never be accidentally re-tracked.
results/

# CEA temporary files
thermo.inp
thermo.lib
thermo.out
tmp.inp
tmp.out
trans.inp
trans.lib
trans.out

# Runtime diagnostic text files (generated during runs)
diagnostics.txt
diagnostics1.txt
diagnostics2.txt

# Python cache
__pycache__/
*.pyc

# LibreOffice lock files
.~lock.*
