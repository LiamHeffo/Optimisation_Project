# Experiment configurations for the X2 MO-CMA-ES driver optimisation.
#
# Each entry configures one call to main().  Fields:
#
#   sim_type     : constraint-handling strategy
#                    'ParentValue'      – infeasible offspring inherit parent's attribute value
#                    'ElitistCrossover' – HV-best parent donates the violating attribute
#                    'RandomCrossover'  – random parent donates the violating attribute
#                    'Penalty'          – infeasible individuals receive a fitness penalty
#                    'CovarianceCHT'    – Chocat 2015 / Arnold-Hansen 2012 covariance
#                                          shrinkage along violation directions.
#                                          Infeasible offspring are NOT repaired
#                                          and NOT evaluated by SPARK / PITOT3;
#                                          their constraint vectors feed an
#                                          eigendecompose-shrink-rescale update
#                                          of each parent's Cholesky factor.
#                                          Per-parent Adaptation-B Mahalanobis
#                                          pooling: each parent's C is updated
#                                          from every infeasible across the swarm,
#                                          weighted by closeness in that parent's
#                                          metric.  See _chtCovarianceUpdate.
#                    'CHT_AL'           – CovarianceCHT (above) PLUS Augmented
#                                          Lagrangian handling of delta_vs1.
#                                          Problem becomes 2-objective
#                                          (hold_time, impact_speed); delta_vs1
#                                          is a constraint adapted via pycma's
#                                          AugmentedLagrangian (algorithm 3,
#                                          Dufossé & Hansen 2020 χ setting).
#                                          MUST set p4_treatment: null and is
#                                          incompatible with the legacy plot
#                                          and CSV layouts (writes its own
#                                          al_diagnostics directory).
#
#   pop_size     : population size  (MU = LAMBDA = pop_size)
#
#   n_gen        : number of generations to run.  Optional; omitted ⇒ 350.
#
#   step_size    : initial CMA-ES step size (sigma), in normalised [1,2]^6 space
#
#   p4_treatment : how to enforce the upper p4 bound after mutation
#                    'hard_bounds_on_p4' – re-transform to stay within the physical p4 limit
#                    null                – no special treatment.  Mandatory for
#                                          'CovarianceCHT' and 'CHT_AL' since
#                                          the CHT replaces repair entirely.
#
#   al_tol       : (CHT_AL only) tolerance ε in m/s for the AL constraint
#                  g_AL(x) = delta_vs1(x) - ε ≤ 0.  Default 100 m/s
#                  (≈ 2% of the 3585 m/s shock-speed target).  Smaller values
#                  drive the AL toward a tighter delta_vs1 at the cost of
#                  longer convergence; larger values relax the constraint.
#
#   cht_gamma    : (CovarianceCHT / CHT_AL) shrinkage strength applied to
#                  each parent's Cholesky factor along violation directions
#                  in _chtCovarianceUpdate.  Higher ⇒ more aggressive
#                  collapse along infeasible directions, faster feasibility
#                  recovery, but risk of over-shrinking and starving
#                  exploration.  Omit the field to use the strategy's
#                  dimension-dependent default 0.5 / (n+2)  (= 0.0625 at n=6).
#                  Reference points along the sweep axis at n=6:
#                    0.00417 = 0.0125/3   (1/3 of Arnold-Hansen 2012 default)
#                    0.0125  = 0.1 / (n+2) (Arnold-Hansen 2012 default)
#                    0.0375  = 0.0125·3   (3× the Arnold-Hansen default)
#                    0.0625  = 0.5 / (n+2) (current strategy default, 5×)
#
#   features     : (optional) dict of anti-degeneration toggles aimed at
#                  preventing σ-collapse in the CHT/AL pipeline.  Each
#                  feature is INDEPENDENT and defaults to off — designed so
#                  any single one can be enabled in isolation to measure
#                  its effect.  Omit the dict (or set keys to null/false)
#                  to reproduce baseline behaviour.
#
#                  Group A — weaken / relax the CHT
#                    cht_eigenvalue_floor: float (e.g. 0.5)
#                        cap per-call shrinkage at (factor·prior_axis).
#                        0.5 ⇒ no axis loses more than 50% per call.
#                    cht_isotropy_alpha: float in (0, 1] (e.g. 0.05)
#                        after CHT shrinkage, pull eigenvalues toward
#                        their mean by α.  Counteracts anisotropy lock-in
#                        across many CHT calls.
#                    cht_gamma_schedule: [start, end, n_gens]
#                                     or  [start, end, start_gen, end_gen]
#                        Schedule for cht_gamma.  3-element form is the
#                        legacy linear-from-gen-0 schedule.  4-element
#                        form holds start until start_gen, interpolates
#                        linearly to end over [start_gen, end_gen], then
#                        stays at end.  Use the 4-element form for
#                        delayed γ-decay (e.g. hold full strength during
#                        the early CHT-pull phase, then back off).
#                    cht_kappa_trigger: float (e.g. 5.0)
#                        when κ(C) exceeds this threshold, re-isotropise
#                        even with no infeasibles present.  Escape valve.
#
#                  Group B — keep AL engaged
#                    al_lam_floor_factor: float (e.g. 0.01)
#                        maintain λ ≥ factor·μ instead of clamping to 0.
#                        Persistent (mild) attraction toward the boundary.
#                    al_mu_decay: float in (0, 1) (e.g. 0.99)
#                        when λ is at its floor, multiply μ by this factor
#                        each gen.  Lets the AL "forget" the early peak.
#                    al_tol_schedule: [start_tol, end_tol, n_gens]
#                                  or  [start_tol, end_tol, start_gen, end_gen]
#                        tighten ε over time.  3-element form is the
#                        legacy linear-from-gen-0 schedule.  4-element
#                        form holds start_tol until start_gen, then
#                        interpolates to end_tol over [start_gen, end_gen],
#                        and stays at end_tol.  Use 4-element for
#                        delayed onset (e.g. let the AL bootstrap on a
#                        wide initial tol, then tighten only once the
#                        search has stabilised).
#                    al_proxy_front_only: bool (e.g. true)
#                        drive the per-gen AL multiplier/penalty update from
#                        the first non-dominated front (w.r.t. AL-augmented
#                        fitness) instead of all surviving parents.  The MO
#                        analogue of pricing "the current solution(s)"
#                        (FRONT-ALAMO, Cocchi/Lapucci/Mansueto 2021).
#                        Affects only the update, NOT the iqr bootstrap.
#                    al_proxy_g_quantile: float in [0, 1] (e.g. 0.75)
#                        aggregate g_al over the proxy set with this upper
#                        quantile instead of the mean.  FRONT-ALAMO uses the
#                        WORST violation; a high quantile is the robust
#                        surrogate for max (mean lets feasible members mask
#                        infeasible ones, stalling the penalty).
#                    al_proxy_front_min_size: int (e.g. 12)
#                        guard on al_proxy_front_only.  Only restrict the
#                        proxy set to the first front when that front holds
#                        at least this many members; otherwise fall back to
#                        all surviving parents.  Defuses the tiny-front
#                        pathology (RCA of al_cht_0088–0091): a high
#                        g-quantile over a handful of front points collapses
#                        to ≈ the worst violator and spuriously arms the AL
#                        as ε tightens.  Matters most for Arnold, whose
#                        fronts stay small.  Omit ⇒ no guard (legacy B4).
#
#                  Group C — redefine / re-treat infeasibility
#                    cht_resample_tol: float (e.g. 0.02)
#                        allow this much constraint violation during the
#                        CHT resample loop.  Reduces CHT-trigger pressure.
#                    box_reflective_repair: bool (true/false)
#                        reflect [1,2]-box violations back into the box at
#                        sampling time, bypassing the CHT for box bounds.
#                    psucc_exclude_resampled: bool (true/false)
#                        exclude CHT-resampled offspring from the donor
#                        parent's σ/psucc adaptation.  Decouples σ from
#                        CHT-induced "successes".
#                    arnold_resample_infeasibles: bool (true/false)
#                        (Arnold family only) after the Eq. 6/7 covariance
#                        shrink, rejection-resample the infeasible slot from
#                        the SHRUNK (σ_i, A_i) until feasible instead of
#                        dropping it.  Hybrid of Arnold's directional
#                        adaptation and rejection sampling — distinct from
#                        Resampling_AL, which redraws from the UNCHANGED
#                        distribution.  Default false (paper drop behaviour).
#                    arnold_resample_max_iterations: int (e.g. 100)
#                        rejection-tail cap for arnold_resample_infeasibles.
#                        A slot still infeasible after the cap is dropped by
#                        selection (same end-state as the drop path).
#                    psucc_sentinel_as_failure: bool (true/false)
#                        partial reversal of Fix-S3.  When True, PITOT3
#                        and SPARK sentinel offspring in the *not-chosen*
#                        branch contribute the standard psucc decay + σ
#                        shrinkage — they are treated as honest failures
#                        rather than skipped.  The chosen-branch Fix-S3
#                        (no "+" signal from sentinel successes) stays
#                        active unconditionally.  Reframes sentinels as
#                        information about region (in)feasibility rather
#                        than noise.  Default False (full Fix-S3).
#
#                  Group F — diversity-preserving fixes (new in run set 11+)
#                    sigma_floor_silent: float (e.g. 1.0e-3)
#                        clamp σ ≥ this value once both AL and CHT have
#                        been silent for ≥ 20 consecutive generations.
#                        Direct backstop against σ collapse in the
#                        post-disengagement window.
#                    selection: 'hv_contribution' | 'crowding'
#                        within-front ranking criterion.
#                        'hv_contribution' (default) is the legacy
#                        Voss 2009 knee-attractive selection.
#                        'crowding' uses NSGA-II crowding distance,
#                        which removes the knee-seeking bias and
#                        directly promotes objective-space diversity.

experiments:
  # ════════════════════════════════════════════════════════════════════════
  # ARNOLD CHT — three staged simulations from the RCA of the al_cht_0088–0091
  # coverage collapse.  Dispatched back-to-back in fresh subprocesses (one per
  # entry, in list order) from the SAME seed population, so the ONLY variable
  # is the constraint-handling treatment:
  #     python3 src/init_population_l1d.py --pop-size 24 ... --launch-cmaes
  #     python3 src/main.py --seed-npz <npz>
  # All three share pop_size=24, step_size=0.1, al_tol=3585, n_gen=500, the
  # ε schedule [3585 → 100 over gens 100..250], HV-indicator selection, and
  # arnold_beta / arnold_cc omitted ⇒ paper defaults 0.1/(n+2), 1/(n+2).
  #
  #   1) LEGACY Arnold  — B4 proxy shaping OFF, so the per-gen AL update is
  #        driven by the mean g_al over ALL surviving parents (this is the
  #        run-86 configuration that produced a well-spread front).  Infeasible
  #        offspring shrink the parent covariance (Eq. 6/7), then the slot is
  #        dropped.
  #   2) FRONT GUARD    — B4 proxy shaping ON (front-only, 0.75 quantile) but
  #        guarded by al_proxy_front_min_size: only summarise the first front
  #        when it holds ≥12 members (μ/2); below that, fall back to all
  #        parents.  Defuses the tiny-front pathology the RCA identified (a
  #        0.75-quantile over ~6 front points ≈ the worst violator, spuriously
  #        arming the AL as ε tightens).  Same drop-on-infeasible as (1).
  #   3) GUARD + RESAMPLE — as (2), plus arnold_resample_infeasibles: an
  #        infeasible slot is rejection-resampled from the Arnold-SHRUNK
  #        (σ_i, A_i) until feasible instead of being dropped.  Hybrid of
  #        Arnold's directional covariance adaptation and rejection sampling
  #        (distinct from Resampling_AL, which redraws from the UNCHANGED
  #        distribution).
  # ════════════════════════════════════════════════════════════════════════
  # 1) Legacy Arnold CHT — B4 off (run-86 proxy)
  - sim_type: ArnoldCHT_AL
    pop_size: 24
    step_size: 0.1
    p4_treatment: null
    al_tol: 3585
    n_gen: 500
    features:
      psucc_sentinel_as_failure: true
      al_tol_schedule: [3585, 100.0, 100, 250]
      # B4 proxy shaping intentionally OFF ⇒ legacy mean-of-all-parents proxy.

  # 2) Arnold CHT — B4 on, guarded against tiny fronts
  - sim_type: ArnoldCHT_AL
    pop_size: 24
    step_size: 0.1
    p4_treatment: null
    al_tol: 3585
    n_gen: 500
    features:
      psucc_sentinel_as_failure: true
      al_tol_schedule: [3585, 100.0, 100, 250]
      al_proxy_front_only: true
      al_proxy_g_quantile: 0.75
      al_proxy_front_min_size: 12    # μ/2 guard — fall back to all parents below this

  # 3) Arnold CHT — guard + resample infeasibles from the shrunk distribution
  - sim_type: ArnoldCHT_AL
    pop_size: 24
    step_size: 0.1
    p4_treatment: null
    al_tol: 3585
    n_gen: 500
    features:
      psucc_sentinel_as_failure: true
      al_tol_schedule: [3585, 100.0, 100, 250]
      al_proxy_front_only: true
      al_proxy_g_quantile: 0.75
      al_proxy_front_min_size: 12
      arnold_resample_infeasibles: true
      arnold_resample_max_iterations: 100   # rejection-tail cap (default 100)


  # ────────────────────────────────────────────────────────────────────────
  # Anti-degeneration feature sweep — each entry enables exactly ONE
  # feature against the same baseline (pop=12, step=0.05, cht_gamma=0.0125,
  # al_tol=100).  Any HV / σ / κ(C) / lam-trajectory difference vs the
  # baseline is attributable to the enabled feature.
  #
  # Comment out the entries you don't want to run.  Each is ~500
  # generations and the runs are dispatched as fresh subprocesses, so
  # the order doesn't matter and you can safely skip any subset.
  # ────────────────────────────────────────────────────────────────────────

  # ────────────────────────────────────────────────────────────────────────
  # Run set 12+ — psucc_sentinel_as_failure exploration (NGEN = 500).
  #
  # All five tests share the new psucc_sentinel_as_failure toggle:
  # SPARK/PITOT3 sentinels in the not-chosen branch contribute σ shrinkage
  # rather than being skipped (treating numerical-failure regions as real
  # information about local feasibility).  Chosen-branch Fix-S3 stays
  # active unconditionally — a sentinel that survives selection is still
  # not a real success.
  #
  # Tests 2-5 also exercise the new 4-element schedule form
  # [start, end, start_gen, end_gen] for delayed-onset schedules:
  # the AL ε holds wide for the first 100 gens (lets the population
  # settle into a feasible region without aggressive constraint
  # pressure), then tightens over gens 100-450 and is constant for
  # the final 50 gens.
  # ────────────────────────────────────────────────────────────────────────

  # T1 — baseline + psucc_sentinel_as_failure.
  # Establishes the new reference: how does the death-spiral look once
  # σ shrinks in response to sentinel failures?  Direct comparison against
  # the pre-toggle baseline (0028).
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 100.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_sentinel_as_failure: true

  # T2 — delayed ε tightening (1500 → 100 over gens 100-450), pop=12.
  # Wide initial tol lets the AL bootstrap on a population that is mostly
  # AL-feasible (delta_vs1 < 1500 m/s), avoiding the early μ over-ramp
  # observed in the baseline rerun.  Tightening starts at gen 100, when
  # the search distribution has had time to align with the real Pareto
  # front, and finishes at gen 450 — the last 50 gens run at ε = 100 to
  # let the population settle on the final constraint surface.
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 1500.0                  # initial; schedule overrides per-gen
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_sentinel_as_failure: true
  #     al_tol_schedule: [1500.0, 100.0, 100, 450]

  # T3 — T2 + γ-decay schedule [0.0125 → 0.0 over gens 100-450], pop=12.
  # CHT shrinkage at full strength during the early feasibility-finding
  # phase (gens 0-99), then linear decay alongside the ε tightening
  # (gens 100-449), then γ = 0 for the final 50 gens.  At γ = 0 the
  # CHT effectively stops carving the covariance — the search runs on
  # pure CMA-ES adaptation, freeing it from the κ inflation observed in
  # the rerun set (κ ≈ 15-18 in 0028 / 0030 was CHT-driven).
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 1500.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_sentinel_as_failure: true
  #     al_tol_schedule:    [1500.0,  100.0, 100, 450]
  #     cht_gamma_schedule: [0.0125,  0.0,   100, 450]

  # T4 — T2 with pop=24.  Tests whether doubling μ (and therefore λ)
  # changes the dynamics: more parents → richer Pareto front candidates,
  # more offspring → more sentinel events per gen but also more
  # information per gen.  pop scaling probes the small-population
  # hypothesis as a separate cause of front clustering.
  # - sim_type: CHT_AL          # (disabled for the seeded ArnoldCHT_AL run)
  #   pop_size: 24
  #   step_size: 0.1
  #   p4_treatment: null
  #   al_tol: 1500.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_sentinel_as_failure: true
  #     al_tol_schedule: [2000.0, 100.0, 100, 250]

  # T5 — T3 with pop=36.  The full stack: psucc-as-failure + delayed ε
  # tightening + γ-decay + doubled population.  Expected to be the
  # highest-budget run of the set and the strongest test of whether
  # the combined mechanisms can produce a well-spread Pareto front
  # without the κ inflation or σ collapse pathologies.
  # - sim_type: CHT_AL          # (disabled for the seeded ArnoldCHT_AL run)
  #   pop_size: 36
  #   step_size: 0.1
  #   p4_treatment: null
  #   al_tol: 1500.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_sentinel_as_failure: true
  #     al_tol_schedule:    [2000.0,  100.0, 100, 250]
  #     cht_gamma_schedule: [0.0125,  0.0,   100, 250]

  # - sim_type: CHT_AL          # (disabled for the seeded ArnoldCHT_AL run)
  #   pop_size: 36
  #   step_size: 0.1
  #   p4_treatment: null
  #   al_tol: 1500.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_sentinel_as_failure: true
  #     al_tol_schedule:    [2000.0,  100.0, 100, 250]
  #     cht_gamma_schedule: [0.0125,  0.0,   100, 250]

  # - sim_type: CHT_AL          # (disabled for the seeded ArnoldCHT_AL run)
  #   pop_size: 36
  #   step_size: 0.1
  #   p4_treatment: null
  #   al_tol: 1500.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_sentinel_as_failure: true
  #     al_tol_schedule:    [2000.0,  100.0, 100, 250]
  #     cht_gamma_schedule: [0.0125,  0.0,   100, 250]

  # - sim_type: CHT_AL          # (disabled for the seeded ArnoldCHT_AL run)
  #   pop_size: 36
  #   step_size: 0.1
  #   p4_treatment: null
  #   al_tol: 1500.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_sentinel_as_failure: true
  #     al_tol_schedule:    [2000.0,  100.0, 100, 250]
  #     cht_gamma_schedule: [0.0125,  0.0,   100, 250]

  # - sim_type: CHT_AL          # (disabled for the seeded ArnoldCHT_AL run)
  #   pop_size: 36
  #   step_size: 0.1
  #   p4_treatment: null
  #   al_tol: 1500.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_sentinel_as_failure: true
  #     al_tol_schedule:    [2000.0,  100.0, 100, 250]
  #     cht_gamma_schedule: [0.0125,  0.0,   100, 250]

  # - sim_type: CHT_AL          # (disabled for the seeded ArnoldCHT_AL run)
  #   pop_size: 36
  #   step_size: 0.1
  #   p4_treatment: null
  #   al_tol: 1500.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_sentinel_as_failure: true
  #     al_tol_schedule:    [2000.0,  100.0, 100, 250]
  #     cht_gamma_schedule: [0.0125,  0.0,   100, 250]

  # ────────────────────────────────────────────────────────────────────────
  # Previous rerun set (0028-0033) — commented out.  These were the
  # post-sentinel-fix runs (baseline + A2 + A3 + B2 + B3 + C3) at
  # NGEN = 400 without the psucc_sentinel_as_failure toggle.  Kept
  # here as a yaml history; uncomment to reproduce.
  # ────────────────────────────────────────────────────────────────────────

  # # Baseline (no features) — pre-toggle.
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 100.0
  #   cht_gamma: 0.0125
  #   features: {}

  # # A2 — isotropy relaxation (5% pull toward mean(eigenvalues) per CHT call).
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 100.0
  #   cht_gamma: 0.0125
  #   features:
  #     cht_isotropy_alpha: 0.05

  # # A3 — gamma schedule (decay 0.0125 → 0.001 over 500 generations).
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 100.0
  #   cht_gamma: 0.0125
  #   features:
  #     cht_gamma_schedule: [0.0125, 0.001, 500]

  # # B2 — mu decay (multiply μ by 0.99 each gen while λ is clamped).
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 100.0
  #   cht_gamma: 0.0125
  #   features:
  #     al_mu_decay: 0.99

  # # B3 — al_tol decay (tighten 100 → 20 m/s over 500 generations).
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 100.0
  #   cht_gamma: 0.0125
  #   features:
  #     al_tol_schedule: [100.0, 20.0, 500]

  # # C3 — exclude resampled offspring from psucc adaptation.
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 100.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_exclude_resampled: true

  # ────────────────────────────────────────────────────────────────────────
  # Diversity-preserving combinations (run set 11+).  Each addresses one
  # or more of the three confirmed damage mechanisms (anisotropy, psucc
  # inflation, frozen-μ boundary sharpening) plus the structural knee-
  # seeking bias of HV-contribution selection.  D1/D2 (archive + Δ
  # output) are universal — they fire on every run regardless of feature
  # flags, so no toggle here.
  #
  # Currently commented out: the rerun-set above is running first
  # (post sentinel-fix), and these combinations will be re-enabled after
  # the rerun analysis (see SENTINEL_FIXES_PLAN.md).
  # ────────────────────────────────────────────────────────────────────────

  # # T11 — σ floor + C3.  Pairs the two σ-retention features: C3 stops
  # # psucc inflation, σ floor backstops if psucc collapses anyway.
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 100.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_exclude_resampled: true
  #     sigma_floor_silent: 1.0e-3

  # # T12 — C3 + A3.  The top mechanism combination: psucc decouple plus
  # # γ-decay schedule.  Both extend σ retention via independent paths.
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 100.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_exclude_resampled: true
  #     cht_gamma_schedule: [0.0125, 0.001, 500]

  # # T13 — C3 + ε schedule with wide start.  Tests the "delayed AL"
  # # hypothesis: start ε wide enough that initial population is mostly
  # # feasible (g_al ≤ 0), tighten over gens 50–250, then constant.
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 1500.0      # initial; overridden by schedule each gen
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_exclude_resampled: true
  #     al_tol_schedule: [1500.0, 100.0, 250]

  # # T14 — Crowding-distance selection on baseline (no other features).
  # # Isolation test: does NSGA-II selection alone meaningfully change
  # # the HV/Δ landscape?  If yes, integrate into T15/T16.
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 100.0
  #   cht_gamma: 0.0125
  #   features:
  #     selection: crowding

  # # T15 — C3 + A3 + crowding-distance selection.
  # # Mechanism stack: psucc decouple (σ retention) + γ schedule (CHT
  # # softening) + crowding selection (anti-knee bias).  Three orthogonal
  # # interventions, one per identified damage mechanism.
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 100.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_exclude_resampled: true
  #     cht_gamma_schedule: [0.0125, 0.001, 500]
  #     selection: crowding

  # # T16 — C3 + A3 + ε schedule.  The alternative triple stack (without
  # # crowding selection), as a comparison point for T15.  Tests whether
  # # the AL/ε mechanism plus γ schedule plus C3 can match the structural
  # # selection fix.
  # - sim_type: CHT_AL
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null
  #   al_tol: 1500.0
  #   cht_gamma: 0.0125
  #   features:
  #     psucc_exclude_resampled: true
  #     cht_gamma_schedule: [0.0125, 0.001, 500]
  #     al_tol_schedule: [1500.0, 100.0, 250]



  # Legacy 3-objective comparators — uncomment to A/B against the AL run.
  # - sim_type: CovarianceCHT
  #   pop_size: 12
  #   step_size: 0.05
  #   p4_treatment: null

  # - sim_type: CovarianceCHT
  #   pop_size: 36
  #   step_size: 0.05
  #   p4_treatment: null

  # - sim_type: CovarianceCHT
  #   pop_size: 12
  #   step_size: 0.5
  #   p4_treatment: null

  # - sim_type: ParentValue
  #   pop_size: 36
  #   step_size: 0.5
  #   p4_treatment: hard_bounds_on_p4

  # - sim_type: ParentValue
  #   pop_size: 36
  #   step_size: 0.5
  #   p4_treatment: hard_bounds_on_p4

